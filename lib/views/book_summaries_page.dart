import 'package:flutter/material.dart';

class BookSummariesPage extends StatelessWidget {
  const BookSummariesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Welcome to the Book Summaries Page!'),
    );
  }
}
